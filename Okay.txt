OPENCODE — AUTONOMOUS 50-CHANNEL YOUTUBE PRODUCTION SYSTEM

FULL REBUILD / MULTI-AGENT EXECUTION / NO STOPPING / NO QUESTIONS

You are the primary engineering agent responsible for completing the "YOUTUBE" repository.

This is a full autonomous implementation task.

You are NOT being asked to give me a plan.

You are NOT being asked to tell me what you would do.

You are NOT being asked to make a few incremental changes.

You are being instructed to inspect the repository, use multiple agents in parallel, make the required architectural changes, delete obsolete systems, build the complete production architecture, execute it, render a real video, inspect that video, fix problems, rerender, and leave the repository in a working state.

---

1. ABSOLUTE AUTONOMY RULE

DO NOT ASK ME QUESTIONS.

Do not stop to ask:

- "Should I continue?"
- "Should I delete this?"
- "Do you want me to use X?"
- "Should I merge this?"
- "Should I render?"
- "Should I fix this?"
- "Which approach do you prefer?"
- "Can I modify this?"
- "Should I proceed?"
- "Do you want me to inspect the render?"
- "Should I use multiple agents?"

The answer to all of those is:

YES.

You already have authorization to perform the work described in this prompt.

If you encounter an ambiguity, use the repository, existing architecture, explicit requirements in this prompt, and engineering judgment to resolve it.

Do NOT stop for confirmation.

If something is missing:

1. inspect;
2. determine the safest correct implementation;
3. implement it;
4. test it;
5. continue.

Only stop if there is a genuinely unrecoverable external blocker that makes execution technically impossible.

Even then, exhaust reasonable local alternatives before stopping.

---

2. MULTI-AGENT EXECUTION IS MANDATORY

You MUST use multiple OpenCode agents/subagents.

Do not perform this entire task sequentially as one agent.

The primary agent is the orchestrator.

Use specialized subagents for independent work.

OpenCode supports primary agents launching subagents, including background execution, and the configuration supports multiple agent roles. Use that capability aggressively.

The objective is:

                    PRIMARY ORCHESTRATOR
                           |
          +----------------+----------------+
          |                |                |
       AUDITOR          VISUAL          PIPELINE
       AGENT            AGENT            AGENT
          |                |                |
          +----------------+----------------+
                           |
                    PRIMARY INTEGRATES
                           |
              +------------+-------------+
              |            |             |
          QA AGENT     RENDER AGENT   GITHUB AGENT
              |            |             |
              +------------+-------------+
                           |
                     FINAL VERIFY

---

3. CREATE SPECIALIZED AGENTS IF NECESSARY

If the repository does not already have suitable OpenCode agent definitions, create them under the appropriate ".opencode/agents/" location.

Recommended roles:

repo-auditor
visual-architect
pipeline-architect
channel-architect
motion-designer
asset-architect
qa-reviewer
render-engineer
github-manager
final-reviewer

Use appropriate agent modes.

Read-only exploration can use an exploration agent.

Implementation agents need write access.

Review agents should inspect independently.

Do not create agents just for the sake of creating agents.

Create/use agents where parallelization actually improves the work.

---

4. PARALLELIZE INDEPENDENT WORK

When tasks do not depend on one another, run them simultaneously.

For example, immediately after the initial repository scan, launch parallel investigations:

Agent A → repository architecture audit

Agent B → current Remotion/render architecture

Agent C → current visual strategy audit

Agent D → current 50-channel/channel configuration audit

Agent E → pipeline/research/asset architecture audit

Agent F → test/QA/render infrastructure audit

Agent G → Git/GitHub branch/status audit

Do NOT make:

A finishes
↓
B starts
↓
B finishes
↓
C starts

when A/B/C are independent.

Use background subagents where appropriate.

The primary agent should continue useful work while background agents are operating.

---

5. DO NOT LET SUBAGENTS BLOCK EACH OTHER UNNECESSARILY

Each agent must have a clearly defined responsibility.

Agents should return:

FINDINGS
FILES INSPECTED
DEPENDENCIES FOUND
PROBLEMS
RECOMMENDATIONS
CHANGES MADE
VERIFICATION

The primary agent must then integrate the results.

If two agents discover overlapping issues, reconcile them.

Do not create conflicting implementations simultaneously.

For write-heavy tasks:

- divide work by subsystem;
- avoid multiple agents editing the same files at the same time;
- let specialized agents work on isolated areas;
- have the primary agent integrate conflicting changes.

---

6. FIRST ACTION: REPOSITORY AUDIT

Before major modifications, inspect the actual repository.

Do not rely on historical descriptions.

Do not assume previous filenames still exist.

Do not assume previous architectures still exist.

Search the entire repository.

Inspect:

package.json
lockfiles
README
src/
compositions/
render scripts
Remotion entry points
visual systems
beat systems
audio systems
SRT handling
asset systems
research systems
channel configuration
provider configuration
tests
QA
scripts
Git configuration
OpenCode configuration

Search for:

render
Remotion
composition
beat
visualPlan
strategy
scene
research
channel
asset
SFX
audio
SRT
caption
subtitle
director
pipeline
provider
API
GitHub

Trace imports.

Trace execution.

Determine what is actually live.

---

7. BUILD AN INTERNAL DEPENDENCY MAP

Classify major code as:

LIVE
LOAD-BEARING
PARTIALLY USED
DUPLICATED
LEGACY
DEAD
BROKEN
REPLACE
PRESERVE

Do not delete anything merely because it looks old.

Do not preserve anything merely because it exists.

The criterion is whether it belongs in the final architecture.

---

8. DELETE AGGRESSIVELY

You have explicit authorization to delete obsolete code.

If something is:

- dead;
- unused;
- superseded;
- duplicated;
- an abandoned experiment;
- a legacy rendering path;
- a primitive-only visual system;
- a duplicate router;
- an obsolete helper;
- an obsolete test path;
- a stale configuration;
- or architecturally harmful,

DELETE IT.

Do not keep dead code because:

«"we might need it later."»

We are finishing the architecture.

However:

Never delete a load-bearing capability before tracing its consumers.

If functionality is valuable but implemented badly:

preserve capability
+
replace implementation

rather than simply removing the capability.

---

9. HISTORICAL FILE NAMES ARE NOT REQUIREMENTS

Previous development iterations may have referenced files such as:

visual/mechanism.js
visual/semantic-qa.js
visual/camera.js
beats/
ImageBeatScene
Chart.jsx
Chip.jsx
Icon.jsx
Node.jsx
Rule.jsx
Layer.jsx
_motion-entry.jsx

These are NOT instructions to recreate them.

If they exist:

- inspect them.

If they don't:

- do not recreate them.

The actual repository is the source of truth.

---

10. THE FINAL ARCHITECTURE

The repository must become:

                    50 CHANNELS
                         |
                CHANNEL REGISTRY
                         |
             CHANNEL-SPECIFIC IDENTITY
                         |
                INDEPENDENT JOB
                         |
        +----------------+----------------+
        |                |                |
     RESEARCH         SCRIPT          VISUAL PLAN
        |                |                |
        +----------------+----------------+
                         |
                   ASSET PLAN
                         |
                ASSET ACQUISITION
                         |
                    COMPOSING
                         |
                      AUDIO
                         |
                   PRE-RENDER QA
                         |
                     RENDER
                         |
                 POST-RENDER QA
                         |
                     COMPLETE

Every production job must be independently executable.

---

11. THE MOST IMPORTANT PIPELINE REQUIREMENT

DO NOT build this:

RESEARCH ALL 50
      ↓
SCRIPT ALL 50
      ↓
ASSETS ALL 50
      ↓
RENDER ALL 50

That architecture is explicitly forbidden.

Build this:

CHANNEL 01
research
→ script
→ visual plan
→ assets
→ compose
→ render
→ QA

CHANNEL 02
research
→ script
→ visual plan
→ assets
→ compose
→ render
→ QA

CHANNEL 03
research
→ script
→ visual plan
→ assets
→ compose
→ render
→ QA

with all jobs able to progress concurrently.

---

12. TRUE PIPELINE INDEPENDENCE

If:

Channel 01 research completes

Channel 01 must immediately continue.

It must NOT wait for:

Channel 02 research
Channel 03 research
...
Channel 50 research

Likewise:

Channel 04 assets complete

must immediately unlock Channel 04 composition.

And:

Channel 09 composition complete

must immediately unlock Channel 09 rendering.

---

13. EXAMPLE OF REQUIRED CONCURRENCY

The system should be capable of reaching a state like:

Channel 01 → RENDERING
Channel 02 → ASSET ACQUISITION
Channel 03 → SCRIPTING
Channel 04 → RESEARCHING
Channel 05 → POST-RENDER QA
Channel 06 → COMPLETE
Channel 07 → RESEARCHING
Channel 08 → COMPOSING

This is correct.

A global phase barrier is not.

---

14. JOB STATE MACHINE

Implement/refactor the production lifecycle around explicit per-job state.

Conceptually:

QUEUED
↓
RESEARCHING
↓
RESEARCH_COMPLETE
↓
SCRIPTING
↓
SCRIPT_COMPLETE
↓
VISUAL_PLANNING
↓
VISUAL_PLAN_COMPLETE
↓
ASSET_ACQUISITION
↓
ASSETS_COMPLETE
↓
COMPOSING
↓
AUDIO
↓
PRE_RENDER_QA
↓
RENDERING
↓
POST_RENDER_QA
↓
COMPLETE

Failures should become isolated job states.

Example:

Channel 17 = ASSET_FAILED

must NOT stop:

Channel 18 = RENDERING
Channel 19 = RESEARCHING
Channel 20 = COMPLETE

---

15. RESUMABILITY

Persist enough state to resume jobs.

If:

Channel 12

fails during rendering, restarting the entire production should not necessarily redo:

- research;
- script;
- asset retrieval;
- audio;
- visual planning.

Resume from the last valid checkpoint.

---

16. 50 CHANNELS — ONE ENGINE, 50 IDENTITIES

Do NOT create 50 separate codebases.

Do NOT create:

channel01Renderer
channel02Renderer
...
channel50Renderer

Instead:

SHARED ENGINE
+
CHANNEL CONFIGURATION
+
VISUAL GRAMMAR
+
CHANNEL RULES
+
STRATEGY SELECTION

Each channel must genuinely affect the resulting video.

Changing only:

name
color
font

is NOT a channel identity.

---

17. CHANNEL CONFIGURATION

Inspect the existing 50 channels.

If a canonical registry already exists, use it.

If not, create one.

Each channel should be able to define things such as:

id:
name:
niche:
audience:
tone:
visual_grammar:
preferred_visual_sources:
secondary_visual_sources:
typography:
camera_language:
motion_language:
cut_rhythm:
sound_language:
asset_policy:
forbidden_patterns:
hook_style:
re_hook_style:
narration_style:
research_style:

Do not duplicate configuration in multiple places.

---

18. MULTIPLE VISUAL GRAMMARS

The system must support different production grammars.

Examples:

DOCUMENTARY
CINEMATIC
NEWS
DATA
TECH / SCREEN
ARCHIVAL
STORYTELLING
EXPLAINER
MONTAGE
TALKING HEAD
HYBRID

A channel can combine grammars.

Examples:

Finance
DOCUMENTARY + DATA + TYPOGRAPHY

Legal
NEWS + DOCUMENTARY + DOCUMENT EVIDENCE

Technology
SCREEN + PRODUCT + CINEMATIC

History
ARCHIVAL + MAP + DOCUMENTARY

Psychology
CINEMATIC + REAL FOOTAGE + TYPOGRAPHY

Philosophy
CINEMATIC + STORYTELLING + TYPOGRAPHY

Science
REAL FOOTAGE + DATA + EXPLAINER

True Crime
DOCUMENTARY + MAP + ARCHIVE

AI
SCREEN + PRODUCT DEMO + DATA

These are examples.

Use the actual repository channel registry.

---

19. VISUAL DIRECTOR

The visual director must be semantic.

Do NOT make the renderer infer everything from raw caption fragments.

Use:

SCRIPT
↓
SEMANTIC BEATS
↓
VISUAL DIRECTOR
↓
VISUAL PLAN
↓
ASSET PLAN
↓
RENDERER

The visual director decides:

WHAT
WHY
MEDIUM
TIMING
CAMERA
MOTION
TYPOGRAPHY
TRANSITION
SOUND RELATIONSHIP

---

20. VISUALS MUST BE MEANINGFUL

Ask:

«What would a professional human editor actually put on screen?»

Possible answers:

- real footage;
- archival footage;
- photograph;
- document;
- map;
- newspaper;
- chart;
- authentic interface;
- screen recording;
- product image;
- physical object;
- cinematic scene;
- timeline;
- data visualization;
- bespoke diagram;
- typography;
- montage.

Do NOT automatically create graphics.

---

21. FORBIDDEN VISUAL LANGUAGE

Avoid:

generic nodes
generic arrows
generic cards
generic rounded rectangles
generic dashboards
icon + headline
random circles
random lines
decorative particles
floating UI
template-like grids
identical compositions
meaningless diagrams

If a relationship is represented with lines simply because the narration contains the word "connected", that is wrong.

If a process is represented as three generic boxes, that is probably wrong.

If a statistic is represented by an arbitrary animated rectangle, that is probably wrong.

The representation must have semantic meaning.

---

22. REAL FOOTAGE

Real footage must be a first-class asset type.

Support appropriate sources such as:

- public-domain archives;
- Wikimedia Commons;
- NASA;
- Archive.org;
- suitable stock;
- licensed media;
- provided footage.

OpenMontage is a useful architectural reference because it treats real footage retrieval, semantic ranking, editing and final rendering as a genuine production path rather than merely animating still images.

Use that idea.

Do NOT copy OpenMontage wholesale.

---

23. NO FULL SUBTITLE SYSTEM

Narration is the main information carrier.

Do NOT turn the video into:

voiceover
+
full subtitles

Visuals should communicate the concept.

Typography should be selective:

- hooks;
- re-hooks;
- strong statements;
- numbers;
- names;
- places;
- labels;
- annotations.

---

24. CAMERA

Camera motion must feel authored.

Use:

HOLD
→
MOVE
→
SETTLE

or:

ESTABLISH
→
FOCUS
→
REVEAL
→
RELEASE

Do not apply one universal zoom curve to every scene.

Camera movement must have a reason.

Different visual grammars should have different camera behavior.

---

25. MOTION

Motion must have purpose.

Use appropriate:

- springs;
- easing;
- anticipation;
- settling;
- stagger;
- parallax;
- depth;
- camera movement;
- material behavior.

Avoid:

- constant bouncing;
- linear everything;
- identical timings;
- everything entering together;
- meaningless movement;
- decorative motion.

---

26. MATERIAL BEHAVIOR

PHOTO:

parallax
crop
camera treatment
grain
subtle lens treatment

DOCUMENT:

physical placement
shadow
camera inspection
annotation

MAP:

geographic movement
zoom
route reveal
location emphasis

DATA:

precise numerical animation
scale-aware transitions

VIDEO:

cuts
reframes
crops
editorial timing

INTERFACE:

authentic state changes
cursor movement
screen transitions

TEXT:

hierarchy
scale
timing
kinetic emphasis

Different materials must feel different.

---

27. AUDIO

Inspect the existing sound system before changing it.

If it is already good, preserve it.

SFX should correspond to:

- material;
- transition;
- scene change;
- impact;
- emphasis;
- environment.

No random click/whoosh spam.

---

28. MULTI-AGENT VISUAL AUDIT

Before rebuilding visual strategies, launch several agents simultaneously.

For example:

Agent 1:
audit DOCUMENTARY / ARCHIVAL / PHOTO strategies

Agent 2:
audit DATA / SCALE / BEFORE-AFTER strategies

Agent 3:
audit RELATIONSHIP / PROCESS / EXPLAINER strategies

Agent 4:
audit INTERFACE / TECH / SCREEN strategies

Agent 5:
audit CINEMATIC / STORYTELLING / TYPOGRAPHY strategies

Agent 6:
audit camera choreography

Agent 7:
audit material identity

Agent 8:
audit mute-readout and semantic correctness

Each agent should independently identify:

what is wrong
why it is wrong
what should replace it
what existing code can be reused
what must be deleted

Then the primary agent integrates the findings.

---

29. DO NOT COSMETICALLY PATCH BAD STRATEGIES

If a strategy is fundamentally wrong:

DELETE/REPLACE IT.

Do not do:

bad diagram
+
gradient
+
glow
+
camera zoom

and call it fixed.

Replace the visual concept.

---

30. REFERENCE IMPLEMENTATIONS

Use concrete open-source references when implementing difficult motion systems.

Inspect real implementations for:

- cinematic cameras;
- kinetic typography;
- documentary montage;
- maps;
- data animation;
- image treatment;
- interface demonstrations;
- physical motion;
- transitions.

Do not blindly copy styles.

Use references to understand implementation quality.

---

31. RESEARCH SYSTEM

Research must produce structured information useful to production.

At minimum:

topic
angle
hook
claims
evidence
sources
visual evidence
footage candidates
photo candidates
map candidates
document candidates
data
uncertainties
licensing notes

Research is not just prose.

Research informs visual production.

---

32. SCRIPT SYSTEM

Scripts must support:

HOOK
↓
CURIOSITY GAP
↓
CONTEXT
↓
ESCALATION
↓
RE-HOOK
↓
PAYOFF
↓
ENDING

Write for the ear.

Do not produce generic introductions.

---

33. ASSET SYSTEM

Every visual beat must determine whether the best medium is:

REAL VIDEO
PHOTO
ARCHIVE
DOCUMENT
MAP
DATA
INTERFACE
PRODUCT
TYPOGRAPHY
GRAPHIC
3D
CINEMATIC
MONTAGE

Asset selection must be semantic.

---

34. FAILURE FALLBACK

If an asset provider fails:

alternative source
→
secondary source
→
safe local asset
→
semantic typography
→
simplified visual

Do not silently replace an important visual with unrelated decorative graphics.

---

35. PROVIDERS

Inspect existing provider architecture.

Use provider abstraction.

Do not hardcode one provider.

Consider:

cost
availability
quality
latency
rate limits
failure
task suitability

Prefer free/local resources where appropriate.

Do not spend money during debugging unnecessarily.

---

36. GITHUB TOKEN

There is a local file named:

GitHub token

Use it if GitHub authentication is required.

NEVER:

- print it;
- log it;
- commit it;
- paste it into source;
- put it into documentation;
- expose it in final output;
- send it to unnecessary external services.

Prefer secure environment/process injection.

Before committing, scan for secret exposure.

---

37. GITHUB OPERATIONS

Inspect:

remote
current branch
main
branches
recent commits
working tree

Use GitHub access to inspect and synchronize repository state where appropriate.

The final production implementation belongs on:

main

Temporary branches may be used during implementation.

Once work is verified and merged, delete obsolete temporary branches when permissions allow.

Do not claim deletion unless it actually occurred.

---

38. TESTING

Use multiple agents for testing too.

For example:

QA Agent A → unit/integration tests

QA Agent B → composition validation

QA Agent C → visual strategy validation

QA Agent D → pipeline state/concurrency validation

QA Agent E → security/secrets validation

The primary agent integrates all results.

---

39. RENDER TEST — MANDATORY

You MUST actually render a video.

Not just build.

Not just test.

Not just compile.

Not just validate composition registration.

A real MP4 must be produced.

The first test should be one channel.

Use the actual production path:

research
→
script
→
visual plan
→
assets
→
audio
→
composition
→
Remotion render
→
post-render QA

Do not create a fake test shortcut that bypasses production.

---

40. RENDER AGENT

Use a dedicated render/QA subagent where practical.

Its responsibility:

execute render
inspect output
extract frames
inspect metadata
inspect audio
identify failures
report exact problems

The primary agent then fixes the implementation.

---

41. RENDER → INSPECT → FIX → RENDER

This loop is mandatory.

IMPLEMENT
↓
RENDER
↓
INSPECT ACTUAL VIDEO
↓
FIND PROBLEMS
↓
FIX
↓
RENDER AGAIN
↓
INSPECT AGAIN

Do not stop after the first successful render if the video is visually poor.

---

42. MUTE TEST

Watch/inspect the rendered video without relying on narration.

Ask:

Can I understand what is happening?
Do visuals correspond to narration?
Are visuals merely decoration?
Is typography carrying too much?
Is the scene visually obvious?

The goal is not complete silent comprehension of every sentence.

The goal is meaningful visual communication.

---

43. PROFESSIONAL VISUAL TEST

Judge the render as a professional editor.

Ask:

Does this look authored?

Does it look like a real edited video?

Is the visual medium appropriate?

Is the camera intentional?

Is motion intentional?

Are materials believable?

Is there enough visual variation?

Does this look like AI-generated template content?

Are we overusing rectangles?

Are we overusing typography?

Are we overusing graphics?

Are real assets used when appropriate?

Does the channel have an identity?

If it fails:

fix it.

---

44. DO NOT STOP AFTER ONE CHANNEL

Once Channel 01 works:

validate the architecture across several channel grammars.

For example:

DOCUMENTARY
DATA
CINEMATIC
TECH
ARCHIVAL
EXPLAINER
NEWS
STORYTELLING
MONTAGE
HYBRID

Use representative channels from the actual 50-channel registry.

---

45. PROVE CONCURRENT PIPELINES

Run several jobs simultaneously.

Demonstrate states such as:

Channel A → RESEARCHING
Channel B → SCRIPTING
Channel C → ASSET ACQUISITION
Channel D → COMPOSING
Channel E → RENDERING
Channel F → POST-RENDER QA

This must be real execution, not documentation.

---

46. PARALLEL AGENT STRATEGY THROUGHOUT THE PROJECT

Continue using multiple agents after the initial audit.

Example:

PRIMARY
│
├── ARCHITECTURE AGENT
├── VISUAL AGENT
├── CHANNEL AGENT
├── PIPELINE AGENT
├── ASSET AGENT
├── QA AGENT
├── RENDER AGENT
└── GITHUB AGENT

Use agents concurrently whenever their work is independent.

Do not force everything through one context window.

---

47. AGENT HANDOFFS

When one agent finishes, its output should be structured.

Example:

STATUS: COMPLETE

INSPECTED:
- file A
- file B
- file C

FINDINGS:
- ...

CHANGES:
- ...

DELETIONS:
- ...

VERIFIED:
- ...

NOT VERIFIED:
- ...

The primary agent should consume these findings and continue.

Do not require the user to relay information between agents.

---

48. NO HUMAN APPROVAL GATES

This project intentionally differs from workflows that stop for creative approval.

Do NOT create:

await user approval

between:

research
script
visual plan
assets
render

The system must operate autonomously.

The user can review the final result afterward.

---

49. NO ARTIFICIAL CHECKPOINT STOPPING

Checkpoints are for:

resumability
state persistence
failure recovery

They are NOT permission gates.

Do not stop at:

checkpoint reached

Continue automatically.

---

50. IF SOMETHING FAILS

Do not immediately report failure.

First:

diagnose
→
retry if safe
→
try fallback
→
inspect logs
→
fix code/config
→
rerun

Only report an unrecoverable blocker after reasonable recovery attempts.

---

51. COST SAFETY

During development:

- use short test renders;
- use low-cost providers;
- reuse assets;
- avoid unnecessary regeneration;
- avoid expensive calls during repeated debugging;
- cache successful outputs where appropriate.

Do not accidentally burn money because the agent keeps regenerating the same asset.

---

52. FINAL CLEANUP

After implementation:

run another multi-agent audit.

Ask agents to independently look for:

dead code
duplicate code
legacy paths
unused imports
duplicate renderers
unused strategies
stale configs
secrets
broken tests
unfinished TODOs
fake fallbacks
generic visuals

Then clean everything proven unnecessary.

---

53. FINAL FULL TEST

Run the complete applicable test suite.

Then:

build
→
composition verification
→
real render
→
post-render QA

Everything that can be verified must be verified.

---

54. FINAL GITHUB STATE

Ensure:

working tree understood
production changes committed
main contains verified implementation
temporary branches cleaned where safe
no secrets committed

Do not fabricate GitHub operations.

---

55. FINAL REPORT

When the work is genuinely finished, provide:

REPOSITORY
- architecture before
- architecture after

DELETED
- files
- directories
- legacy systems

CREATED
- files
- systems
- agents
- pipeline infrastructure

50 CHANNELS
- registry
- identities
- visual grammars

PIPELINE
- independent job lifecycle
- concurrency
- failure isolation
- resumability

VISUALS
- strategy system
- real footage
- asset types
- camera
- motion
- typography
- materials

TESTING
- tests run
- results

RENDER
- exact render executed
- output path
- duration
- resolution
- QA result

GITHUB
- branch
- commit
- cleanup

REMAINING
- only genuinely unresolved items

For every result distinguish:

VERIFIED

from:

NOT VERIFIED

Never claim something is verified when it was not actually tested.

---

56. THE FINAL OBJECTIVE

When you finish, the repository should no longer feel like a collection of experiments.

It should feel like one coherent production system.

The target architecture is:

                         YOUTUBE
                            |
                  +---------+---------+
                  |                   |
             50 CHANNELS         SHARED ENGINE
                  |                   |
          CHANNEL IDENTITY      PIPELINE ENGINE
                  |                   |
                  +---------+---------+
                            |
                     INDEPENDENT JOBS
                            |
        +-------------------+-------------------+
        |                   |                   |
     RESEARCH            SCRIPT             VISUALS
        |                   |                   |
        +-------------------+-------------------+
                            |
                         ASSETS
                            |
                         AUDIO
                            |
                        COMPOSE
                            |
                           QA
                            |
                         RENDER
                            |
                      POST-REVIEW
                            |
                         VIDEO

And those jobs must be able to operate concurrently.

The production system should be capable of:

Channel 01 → researching
Channel 02 → scripting
Channel 03 → collecting footage
Channel 04 → composing
Channel 05 → rendering
Channel 06 → QA

at the same time.

---

57. EXECUTION COMMAND

Start now.

Do not ask for confirmation.

Do not wait for me.

Do not return a plan and stop.

Do not give me a list of things I should do.

You are the implementation agent.

Use multiple subagents immediately.

Audit the repository.

Build the dependency map.

Delete obsolete systems.

Implement the final architecture.

Run the tests.

Run the actual production pipeline.

Render a real video.

Inspect the actual rendered output.

Fix what is wrong.

Render again.

Validate independent channel execution.

Clean the repository.

Commit the verified work.

Leave "main" in a production-ready state.

Then give me the final verified report.

DO THE WORK.
