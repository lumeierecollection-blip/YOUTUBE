// Director module - PART 5 visual system rebuild
// AUTHORED beat -> [ DIRECTOR ] -> VISUAL PLAN -> states.js -> SCENE
// THREE INPUT PATHS, IN STRICT PRIORITY ORDER (PART 13)
// line1
// line2
// line3
// simple line1
// simple line2
// The director

// ranks detector signals

// selects the best strategy

// confirms data requirements are met

// from beat own signals first

// Context signals are only consulted when beat own signals produce nothing

// Beat owns signals (trusted -- they belong to this specific beat)

// ranking: beat signals first, then context, then fallback

// Find highest confidence signal from beat own signals

// Build visual plan object with strategy, concept, and data

// Create deterministic visual plan for states.js and scene components

