# Style reference — The Engineering Archive (ch-35, cinematic-documentary)

**This is a derivation record, not a research report.** ch-01, ch-02 and
ch-09 have documents of the other kind: fields traced to real external
references that were fetched and cited. This channel's specification was
derived from the curated `visual_spec` block already in
`config/channels.json`, and every row below names the text it came from.
Treat it as sound provenance for a first render and as a candidate for
replacement by real reference research.

Generated by `scripts/build-visual-identity.js` from
`config/visual-identity.source.json` + `config/channels.json`.

## Where each field came from

| Field | Value | Source |
|---|---|---|
| primary_palette | #05051A, #0A0A30, #60A5FA, #FFFFFF | `colors.primary/secondary/accent`, then the `bg_mode: white` ground from `styles/tokens.js` |
| secondary_palette | #4B81C3, #8F8F98, #EBEBED | computed: accent x 0.78, mix(primary,white,0.55), mix(primary,white,0.92) |
| typography_primary | Space Grotesk | `font` |
| typography_secondary | JetBrains Mono | authored contrasting face, woff2 present in `public/fonts` |
| environment_type | machine-mechanism | b_roll: custom 3D animations, data visualizations, real engineering footage, CAD models. cameras: 3D tech orbit (a lateral track), process flow, data visualization, scale comparison. |
| core_objects | gear train, cross section, load curve, bolt joint, blueprint sheet, scale bar | drawn from the same b_roll and camera list |
| camera_language | track-right, pan-left, push-in, high-angle | `visual_spec.camera_angles` read into the twelve-move vocabulary |
| transition_language | 3d-flythrough, data-morph, cross-dissolve-tech | first three of `visual_spec.transitions` |
| framing_default | medium | authored from the niche and camera list |
| text_placement | corner-overlay | authored from the niche and camera list |
| use_of_negative_space | medium | authored from `visual_spec.color_grade` and style |
| motion_curve | ease-in | pacing: measured with technical crescendos |

## The channel's own words

- **niche** — Real Engineering — Cinematic Tech Deep Dives
- **tone** — calm-authoritative
- **color_grade** — cool blue/dark tech palette with bright highlights
- **pacing** — measured with technical crescendos
- **b_roll_sources** — custom 3D animations; stock footage; data visualizations; real engineering footage; CAD models
- **camera_angles** — 3D tech orbit; process flow; data visualization; real footage cutaway; scale comparison
- **transitions** — 3d-flythrough; data-morph; cross-dissolve-tech; concept-reveal
