# Style reference — Skill Stack (ch-44, motion-graphics)

**This is a derivation record, not a research report.** ch-01, ch-02 and
ch-09 have documents of the other kind: fields traced to real external
references that were fetched and cited. This channel's specification was
derived from the curated `visual_spec` block already in
`config/channels.json`, and every row below names the text it came from.
Treat it as sound provenance for a first render and as a candidate for
replacement by real reference research.

Generated by `scripts/build-visual-identity.js` from
`config/visual-identity.source.json` + `config/channels.json`.

## Where each field came from

| Field | Value | Source |
|---|---|---|
| primary_palette | #0F172A, #1E293B, #A78BFA, #FFFFFF | `colors.primary/secondary/accent`, then the `bg_mode: white` ground from `styles/tokens.js` |
| secondary_palette | #826CC3, #93979F, #ECECEE | computed: accent x 0.78, mix(primary,white,0.55), mix(primary,white,0.92) |
| typography_primary | Inter | `font` |
| typography_secondary | Space Grotesk | authored contrasting face, woff2 present in `public/fonts` |
| environment_type | abstract-space | b_roll: custom vector illustrations (Kurzgesagt style), infographic animations, concept visualizations, diagram builds. cameras: concept animation, infographic build, data visualization, metaphor illustration. |
| core_objects | concept node, link path, stacked layer, progress arc, figure silhouette, stepped platform | drawn from the same b_roll and camera list |
| camera_language | static, push-in, pan-left, pull-out | `visual_spec.camera_angles` read into the twelve-move vocabulary |
| transition_language | morph-concept, infographic-reveal, concept-zoom | first three of `visual_spec.transitions` |
| framing_default | medium | authored from the niche and camera list |
| text_placement | centre | authored from the niche and camera list |
| use_of_negative_space | high | authored from `visual_spec.color_grade` and style |
| motion_curve | ease-out | pacing: fast but readable, pattern interrupts every 90-120 sec |

## The channel's own words

- **niche** — Professional Skill Development
- **tone** — insightful-empowering
- **color_grade** — bright Kurzgesagt-style flat 2D vector with purple accent highlights, layered parallax depth, cinematic zoom sequences
- **pacing** — fast but readable, pattern interrupts every 90-120 sec, 3-5 sec visual changes
- **b_roll_sources** — custom vector illustrations (Kurzgesagt style); stock office footage; infographic animations; concept visualizations; diagram builds
- **camera_angles** — concept animation; infographic build; stock cutaway; data visualization; metaphor illustration
- **transitions** — morph-concept; infographic-reveal; concept-zoom; parallax-shift; vector-morph
