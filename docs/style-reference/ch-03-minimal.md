# Style reference — AI Tested (ch-03, minimal)

**This is a derivation record, not a research report.** ch-01, ch-02 and
ch-09 have documents of the other kind: fields traced to real external
references that were fetched and cited. This channel's specification was
derived from the curated `visual_spec` block already in
`config/channels.json`, and every row below names the text it came from.
Treat it as sound provenance for a first render and as a candidate for
replacement by real reference research.

Generated by `scripts/build-visual-identity.js` from
`config/visual-identity.source.json` + `config/channels.json`.

## Where each field came from

| Field | Value | Source |
|---|---|---|
| primary_palette | #0A0A1A, #1A1A3A, #8B5CF6, #000000 | `colors.primary/secondary/accent`, then the `bg_mode: black` ground from `styles/tokens.js` |
| secondary_palette | #6C48C0, #919198, #EBEBED | computed: accent x 0.78, mix(primary,white,0.55), mix(primary,white,0.92) |
| typography_primary | Space Grotesk | `font` |
| typography_secondary | JetBrains Mono | authored contrasting face, woff2 present in `public/fonts` |
| environment_type | screen-interface | b_roll: screen recordings of AI tools, output comparisons, pricing page screenshots. cameras: screen capture primary, split-screen comparison, output zoom-in, pricing table. |
| core_objects | application window, output transcript, benchmark bar, prompt field, latency trace, cursor pointer | drawn from the same b_roll and camera list |
| camera_language | static, push-in, pan-right, high-angle | `visual_spec.camera_angles` read into the twelve-move vocabulary |
| transition_language | screen-switch, split-reveal, glitch-cut | first three of `visual_spec.transitions` |
| framing_default | medium | authored from the niche and camera list |
| text_placement | upper-third | authored from the niche and camera list |
| use_of_negative_space | high | authored from `visual_spec.color_grade` and style |
| motion_curve | ease-out | pacing: fast-paced with clear comparisons |

## The channel's own words

- **niche** — AI Tool Reviews (Real Results Only)
- **tone** — casual-authoritative
- **color_grade** — dark mode with purple accent glows, clean tech aesthetic
- **pacing** — fast-paced with clear comparisons
- **b_roll_sources** — screen recordings of AI tools; output comparisons; pricing page screenshots; workflow demonstrations; stock tech footage
- **camera_angles** — screen capture primary; split-screen comparison; output zoom-in; pricing table; workflow diagram
- **transitions** — screen-switch; split-reveal; glitch-cut
