# Style reference — Interview Insider (ch-46, minimal)

**This is a derivation record, not a research report.** ch-01, ch-02 and
ch-09 have documents of the other kind: fields traced to real external
references that were fetched and cited. This channel's specification was
derived from the curated `visual_spec` block already in
`config/channels.json`, and every row below names the text it came from.
Treat it as sound provenance for a first render and as a candidate for
replacement by real reference research.

Generated by `scripts/build-visual-identity.js` from
`config/visual-identity.source.json` + `config/channels.json`.

## Where each field came from

| Field | Value | Source |
|---|---|---|
| primary_palette | #0F172A, #1E293B, #22C55E, #FFFFFF | `colors.primary/secondary/accent`, then the `bg_mode: white` ground from `styles/tokens.js` |
| secondary_palette | #1B9A49, #93979F, #ECECEE | computed: accent x 0.78, mix(primary,white,0.55), mix(primary,white,0.92) |
| typography_primary | DM Sans | `font` |
| typography_secondary | Playfair Display | authored contrasting face, woff2 present in `public/fonts` |
| environment_type | interior-room | b_roll: stock corporate footage, text overlays, question/answer graphics. cameras: medium talking head equivalent, text question display, answer framework, pass/fail indicator. |
| core_objects | answer frame, checklist rule, figure silhouette, question line, desk edge, clock face | drawn from the same b_roll and camera list |
| camera_language | static, push-in, tilt-up, pan-right | `visual_spec.camera_angles` read into the twelve-move vocabulary |
| transition_language | simple-cut, fade-section, text-card | first three of `visual_spec.transitions` |
| framing_default | medium | authored from the niche and camera list |
| text_placement | centre | authored from the niche and camera list |
| use_of_negative_space | high | authored from `visual_spec.color_grade` and style |
| motion_curve | linear | pacing: steady, allows each tip to land |

## The channel's own words

- **niche** — Career & Interview Prep
- **tone** — professional-encouraging
- **color_grade** — clean professional, neutral, well-lit
- **pacing** — steady, allows each tip to land
- **b_roll_sources** — stock corporate footage; screen recordings; text overlays; question/answer graphics; infographic animations
- **camera_angles** — medium talking head equivalent; text question display; answer framework; pass/fail indicator; stock cutaway
- **transitions** — simple-cut; fade-section; text-card; number-reveal
