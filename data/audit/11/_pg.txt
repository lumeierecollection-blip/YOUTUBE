23: *   8+3·i    gridline i DRAW 10f each (the live chart has exactly THREE
24: *            gridlines at 25/50/75 % of the plot — SFR-motion-5; the A4
54: * axis/gridline re-animation, no letter-spacing/weight/radius animation.
87: const STROKE = 2; // bar border + gridline stroke width
195: // A4 construction rows — baseline, gridlines, axis label.
197: const gridlineP = (i) =>
215: data-role="baseline"
226: {/* E3.4 order 2 — horizontal gridlines at 25/50/75 % of the plot, DRAW
231: data-role="gridline"
237: width: `${DRAW_END * gridlineP(i)}%`,
239: opacity: GRID_OPACITY,
245: translateY +24 → 0 over D.base, opacity 0 → 1 over D.short, E.out).
247: the gridline (L10), vertically centred on the plot (E3.5). */}
250: data-role="axis-label"
258: opacity: axisLabelOpacity,
348: opacity: labelOpacity,